# Feedback

### DB backup file included
> import db bacpack file before project build

### Run the below command in pm console upon build failed
```
Update-Package Microsoft.CodeDom.Providers.DotNetCompilerPlatform -r
```
